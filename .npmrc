# Required because react-dropzone@15 declares a malformed peer range
# (">= 16.8 || 18.0.0") that excludes React 19 under strict resolution.
# Remove this when react-dropzone ships a corrected peerDependencies field.
legacy-peer-deps=true
